let btnSend = document.getElementById('sendmsg')
let inputName = document.getElementById('name').value
const changeName = () => {
    let nombre = inputName
    console.log(nombre);
}

// inputName.addEventListener('click', changeName);